<Page
    x:Class="ZumenSearch.Views.Rent.Residentials.Editor.TransportationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:mod="using:ZumenSearch.Models"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:animations="using:CommunityToolkit.WinUI.Animations"
    NavigationCacheMode="Required">
    <Page.Resources>
        <animations:ImplicitAnimationSet x:Name="ShowTransitions">
            <animations:OffsetAnimation EasingMode="EaseOut" From="0,24,0" To="0" Duration="0:0:0.4" />
            <animations:OpacityAnimation EasingMode="EaseOut" From="0" To="1" Duration="0:0:0.2" />
        </animations:ImplicitAnimationSet>
        <animations:ImplicitAnimationSet x:Name="HideTransitions">
            <animations:OffsetAnimation EasingMode="EaseOut" From="0" To="0,24,0" Duration="0:0:0.2" />
            <animations:OpacityAnimation EasingMode="EaseOut" From="1" To="0" Duration="0:0:0.1" />
        </animations:ImplicitAnimationSet>
    </Page.Resources>

    <Grid x:Name="ContentArea">
        <StackPanel Spacing="12" Margin="0,24,0,0">

            <controls:Segmented x:Name="segmentedControl" HorizontalAlignment="Center" SelectedIndex="0">
                <controls:SegmentedItem Content="交通１" Tag="one" />
                <controls:SegmentedItem Content="交通２" Tag="two" />
                <controls:SegmentedItem Content="交通３" Tag="three" />
                <controls:SegmentedItem Content="交通４" Tag="four" />
            </controls:Segmented>
            
            <controls:SwitchPresenter Value="{Binding SelectedItem.Tag, ElementName=segmentedControl}">
                <controls:Case Value="one">
                    <Border animations:Implicit.HideAnimations="{StaticResource HideTransitions}"
                            animations:Implicit.ShowAnimations="{StaticResource ShowTransitions}"
                            HorizontalAlignment="Stretch"  Style="{StaticResource RoundCardBorderStyle}">

                        <StackPanel Spacing="12" HorizontalAlignment="Stretch">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*" MinWidth="320"/>
                                    <ColumnDefinition Width="1*" MinWidth="320"/>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid Grid.Column="0">
                                        <TextBox x:Name="TextBoxRailLine" Header="路線" IsReadOnly="True" IsEnabled="False" TextChanged="TextBoxRailLine_TextChanged" Text="{x:Bind ViewModel.SelectedRailLine.LineName, Mode=OneWay}" Margin="0" HorizontalAlignment="Stretch"></TextBox>
                                    </Grid>
                                    <Grid Grid.Column="1" Margin="6,0,0,0">
                                        <Button VerticalAlignment="Bottom" Content="選択" Command="{x:Bind ViewModel.ShowRailLineSelectCommand, Mode=OneWay}"></Button>
                                    </Grid>
                                </Grid>
                                <Grid Grid.Column="1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid Grid.Column="0">
                                        <TextBox x:Name="TextBoxRailStation" Header="駅名" IsReadOnly="True" IsEnabled="False" TextChanged="TextBoxRailStation_TextChanged" Text="{x:Bind ViewModel.SelectedRailStation.StationName, Mode=OneWay}" Margin="12,0,0,0" HorizontalAlignment="Stretch"></TextBox>
                                    </Grid>
                                    <Grid Grid.Column="1" Margin="6,0,0,0">
                                        <Button VerticalAlignment="Bottom" Content="選択" Command="{x:Bind ViewModel.ShowRailStationSelectCommand, Mode=OneWay}"></Button>
                                    </Grid>
                                </Grid>
                            </Grid>
                            <NumberBox Header="駅から徒歩（分）" Value="{x:Bind ViewModel.EkiToho, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MinWidth="90" Margin="0" HorizontalAlignment="Left"></NumberBox>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*" MinWidth="320"/>
                                    <ColumnDefinition Width="1*" MinWidth="320"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"></RowDefinition>
                                    <RowDefinition Height="Auto"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Grid Grid.Row="0" Grid.Column="0">
                                    <TextBox Header="バス停名" Text="{x:Bind ViewModel.BusStop, Mode=TwoWay}" HorizontalAlignment="Stretch"></TextBox>
                                </Grid>
                                <Grid Grid.Row="1" Grid.Column="0">
                                    <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,12,0,0">
                                        <NumberBox Header="バス乗車時間（分）" Value="{x:Bind ViewModel.BusJyousya, Mode=TwoWay}" MinWidth="90" Margin="0" HorizontalAlignment="Left"></NumberBox>
                                        <NumberBox Header="バス停から徒歩（分）" Value="{x:Bind ViewModel.BusStopToho, Mode=TwoWay}" MinWidth="90" Margin="12,0,0,0" HorizontalAlignment="Left"></NumberBox>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </StackPanel>

                    </Border>
                </controls:Case>
                <controls:Case Value="two">
                    <Border animations:Implicit.HideAnimations="{StaticResource HideTransitions}"
                            animations:Implicit.ShowAnimations="{StaticResource ShowTransitions}"
                            HorizontalAlignment="Stretch"  Style="{StaticResource RoundCardBorderStyle}">

                        <TextBlock VerticalAlignment="Center"
                               Text="This is the two panel" />
                    </Border>
                </controls:Case>
                <controls:Case Value="three">
                    <Border animations:Implicit.HideAnimations="{StaticResource HideTransitions}"
                            animations:Implicit.ShowAnimations="{StaticResource ShowTransitions}"
                            HorizontalAlignment="Stretch"  Style="{StaticResource RoundCardBorderStyle}">

                        <TextBlock VerticalAlignment="Center"
                               Text="This is the three panel" />
                    </Border>
                </controls:Case>
                <controls:Case Value="four">
                    <Border animations:Implicit.HideAnimations="{StaticResource HideTransitions}"
                            animations:Implicit.ShowAnimations="{StaticResource ShowTransitions}"
                            HorizontalAlignment="Stretch"  Style="{StaticResource RoundCardBorderStyle}">

                        <TextBlock VerticalAlignment="Center"
                               Text="This is the four panel" />
                    </Border>
                </controls:Case>
            </controls:SwitchPresenter>

        </StackPanel>
    </Grid>
</Page>

