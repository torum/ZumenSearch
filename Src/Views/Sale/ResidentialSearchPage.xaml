<Page
    x:Class="ZumenSearch.Views.Sale.ResidentialSearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d"
    xmlns:modcommon="using:ZumenSearch.Models.Common"
    NavigationCacheMode="Required">

    <Grid Padding="36">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Text="売買住居用"
            FontSize="28"
            FontWeight="SemiBold" />

        <StackPanel
            Grid.Row="1"
            Margin="0,24,0,0"
            Orientation="Horizontal"
            Spacing="12">

            <TextBox
                x:Name="SearchTextBox"
                Width="360"
                Header="物件名"
                PlaceholderText="物件名を入力" />

            <Button
                Content="検索"
                VerticalAlignment="Bottom"
                Click="Search_Click" />

            <Button
                Content="新規"
                VerticalAlignment="Bottom"
                Command="{x:Bind ViewModel.AddNewSaleResidentialBldgCommand}" />
        </StackPanel>

        <TextBlock
            Grid.Row="2"
            Margin="0,32,0,0"
            Text="検索条件を入力して検索してください。" />
    </Grid>

</Page>
