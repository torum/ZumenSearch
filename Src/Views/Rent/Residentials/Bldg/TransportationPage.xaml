<Page
    x:Class="ZumenSearch.Views.Rent.Residentials.Bldg.TransportationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:mod="using:ZumenSearch.Models.Common"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:animations="using:CommunityToolkit.WinUI.Animations" 
    xmlns:framework="using:Microsoft.UI.Xaml"
    NavigationCacheMode="Required">
    <Page.Resources>
        <animations:ImplicitAnimationSet x:Name="ShowTransitions">
            <animations:OffsetAnimation EasingMode="EaseOut" From="0,24,0" To="0" Duration="0:0:0.4" />
            <animations:OpacityAnimation EasingMode="EaseOut" From="0" To="1" Duration="0:0:0.2" />
        </animations:ImplicitAnimationSet>
        <animations:ImplicitAnimationSet x:Name="HideTransitions">
            <animations:OffsetAnimation EasingMode="EaseOut" From="0" To="0,24,0" Duration="0:0:0.2" />
            <animations:OpacityAnimation EasingMode="EaseOut" From="1" To="0" Duration="0:0:0.1" />
        </animations:ImplicitAnimationSet>
    </Page.Resources>
    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" HorizontalScrollMode="Disabled" Padding="48,0,48,24">
        <Grid x:Name="ContentArea">
            <StackPanel Spacing="12" Margin="0,24,0,0">

                <controls:Segmented x:Name="SegmentedControl" HorizontalAlignment="Center" SelectedIndex="0">
                    <controls:SegmentedItem Content="交通１" Tag="one" />
                    <controls:SegmentedItem Content="交通２" Tag="two" />
                    <controls:SegmentedItem Content="交通３" Tag="three" />
                    <controls:SegmentedItem Content="交通４" Tag="four" />
                </controls:Segmented>

                <controls:SwitchPresenter Value="{x:Bind ((framework:FrameworkElement)SegmentedControl.SelectedItem).Tag, Mode=OneWay}">
                    <controls:Case Value="one">
                        <Border animations:Implicit.HideAnimations="{StaticResource HideTransitions}"
                            animations:Implicit.ShowAnimations="{StaticResource ShowTransitions}"
                            HorizontalAlignment="Stretch"  Style="{StaticResource RoundCardBorderStyle}">

                            <StackPanel Spacing="12" HorizontalAlignment="Stretch">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*" MinWidth="320"/>
                                        <ColumnDefinition Width="1*" MinWidth="320"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid Grid.Column="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column="0">
                                            <TextBox x:Name="TextBoxRailLine1" Header="路線" IsReadOnly="True" IsEnabled="False" TextChanged="TextBoxRailLine1_TextChanged" Text="{x:Bind ViewModel.Bldg.SelectedRailLine1.LineName, Mode=OneWay}" Margin="0" HorizontalAlignment="Stretch"></TextBox>
                                        </Grid>
                                        <Grid Grid.Column="1" Margin="6,0,0,0">
                                            <Button VerticalAlignment="Bottom" Content="選択" Command="{x:Bind ViewModel.Bldg.ShowRailLineSelect1Command, Mode=OneWay}"></Button>
                                        </Grid>
                                    </Grid>
                                    <Grid Grid.Column="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column="0">
                                            <TextBox x:Name="TextBoxRailStation1" Header="駅名" IsReadOnly="True" IsEnabled="False" TextChanged="TextBoxRailStation1_TextChanged" Text="{x:Bind ViewModel.Bldg.SelectedRailStation1.StationName, Mode=OneWay}" Margin="12,0,0,0" HorizontalAlignment="Stretch"></TextBox>
                                        </Grid>
                                        <Grid Grid.Column="1" Margin="6,0,0,0">
                                            <Button VerticalAlignment="Bottom" Content="選択" Command="{x:Bind ViewModel.Bldg.ShowRailStationSelect1Command, Mode=OneWay}"></Button>
                                        </Grid>
                                    </Grid>
                                </Grid>
                                <Grid>
                                    <TextBox Header="駅から徒歩（分）" PlaceholderText="半角英数" Text="{x:Bind ViewModel.Bldg.EkiToho1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MinWidth="90" Margin="0" HorizontalAlignment="Left"></TextBox>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*" MinWidth="320"/>
                                        <ColumnDefinition Width="1*" MinWidth="320"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"></RowDefinition>
                                        <RowDefinition Height="Auto"></RowDefinition>
                                    </Grid.RowDefinitions>
                                    <Grid Grid.Row="0" Grid.Column="0">
                                        <TextBox Header="バス停名" Text="{x:Bind ViewModel.Bldg.BusStop, Mode=TwoWay}" HorizontalAlignment="Stretch"></TextBox>
                                    </Grid>
                                    <Grid Grid.Row="1" Grid.Column="0">
                                        <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,12,0,0">
                                            <TextBox Header="バス乗車時間（分）" PlaceholderText="半角英数" Text="{x:Bind ViewModel.Bldg.BusJyousya1, Mode=TwoWay}" MinWidth="90" Margin="0" HorizontalAlignment="Left"></TextBox>
                                            <TextBox Header="バス停から徒歩（分）" PlaceholderText="半角英数" Text="{x:Bind ViewModel.Bldg.BusStopToho1, Mode=TwoWay}" MinWidth="90" Margin="12,0,0,0" HorizontalAlignment="Left"></TextBox>
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </StackPanel>

                        </Border>
                    </controls:Case>
                    <controls:Case Value="two">
                        <Border animations:Implicit.HideAnimations="{StaticResource HideTransitions}"
                            animations:Implicit.ShowAnimations="{StaticResource ShowTransitions}"
                            HorizontalAlignment="Stretch"  Style="{StaticResource RoundCardBorderStyle}">

                            <TextBlock VerticalAlignment="Center"
                               Text="This is the two panel" />
                        </Border>
                    </controls:Case>
                    <controls:Case Value="three">
                        <Border animations:Implicit.HideAnimations="{StaticResource HideTransitions}"
                            animations:Implicit.ShowAnimations="{StaticResource ShowTransitions}"
                            HorizontalAlignment="Stretch"  Style="{StaticResource RoundCardBorderStyle}">

                            <TextBlock VerticalAlignment="Center"
                               Text="This is the three panel" />
                        </Border>
                    </controls:Case>
                    <controls:Case Value="four">
                        <Border animations:Implicit.HideAnimations="{StaticResource HideTransitions}"
                            animations:Implicit.ShowAnimations="{StaticResource ShowTransitions}"
                            HorizontalAlignment="Stretch"  Style="{StaticResource RoundCardBorderStyle}">

                            <TextBlock VerticalAlignment="Center"
                               Text="This is the four panel" />
                        </Border>
                    </controls:Case>
                </controls:SwitchPresenter>

            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>

