<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ZumenSearch.Views.Dialogs.RailLineSelectPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ZumenSearch.Views.Dialogs"
    xmlns:mod="using:ZumenSearch.Models.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <TextBox x:Name="TextBoxRailLine" Header="路線名" Text="{x:Bind ViewModel.Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0" HorizontalAlignment="Stretch" VerticalAlignment="Bottom">
                    <TextBox.KeyboardAccelerators>
                        <KeyboardAccelerator Key="Enter" Invoked="KeyboardAccelerator_Invoked" />
                    </TextBox.KeyboardAccelerators>
                </TextBox>
            </Grid>
            <Grid Grid.Column="1" Margin="6,0,0,0">
                <Button Content="検索" VerticalAlignment="Bottom" Command="{x:Bind ViewModel.SearchRailLineCommand}"></Button>
            </Grid>
        </Grid>
        <Grid Grid.Row="1" Margin="0,6,0,0">
            <Border Style="{StaticResource RoundCardBorderStyle}" Padding="0">
                <ListView x:Name="SelectionListView" ItemsSource="{x:Bind ViewModel.SuggestedRailLines, Mode=OneWay}" SelectionMode="Single" SelectedItem="{x:Bind ViewModel.SelectedRailLine,Mode=TwoWay}" Margin="0,6,0,6" Height="150" Width="260">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="mod:RailLine">
                            <TextBlock Text="{x:Bind LineName}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"></TextBlock>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </Grid>

    </Grid>
</Page>
